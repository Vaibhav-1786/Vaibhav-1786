# Animation Note

`profile.gif` uses the supplied Vaibhav Chauhan artwork with a subtle cinematic animation. The README uses GIF because animated SVGs are not reliably animated by GitHub's README renderer.
